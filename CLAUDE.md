@AGENTS.md

# Claude Code compatibility

`AGENTS.md` is the canonical shared instruction file for this repository.

Before substantial work:
- follow `AGENTS.md`;
- read any scoped rules referenced there;
- check for a more specific `AGENTS.md` in the directory you are modifying.

Use planning/review before broad changes. Do not duplicate the shared policy into this file.

For personal project-specific preferences, use `CLAUDE.local.md` and keep it uncommitted.
